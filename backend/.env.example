MONGO_URI=mongodb://localhost:27017/charkha
JWT_SECRET=change-me
PORT=8000

# Frontend origin(s) for CORS (comma-separated not supported — use CORS_ALLOW_ALL for dev).
CORS_ORIGIN=http://localhost:3000

# Allow any origin with credentials (useful for local dev).
# CORS_ALLOW_ALL=true

# Required for direct browser → API calls (NEXT_PUBLIC_API_URL on frontend).
COOKIE_CROSS_SITE=true
